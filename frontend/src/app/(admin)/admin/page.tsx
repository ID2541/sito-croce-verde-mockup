import { AdminDemoNotice } from "@/components/admin/AdminDemoNotice";

export default function AdminEntryPage() {
  return (
    <AdminDemoNotice
      title="Area admin"
      description="Questa schermata rappresenta l'hub del futuro back office editoriale. Nel prodotto finale conterra dashboard, gestione contenuti e accessi per ruoli autorizzati."
    />
  );
}
