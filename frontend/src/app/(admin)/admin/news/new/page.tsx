import { AdminDemoNotice } from "@/components/admin/AdminDemoNotice";

export default function AdminNewsNewPage() {
  return (
    <AdminDemoNotice
      title="Nuova news"
      description="Questa vista definisce lo spazio per il form di creazione contenuti. Nel mock resta intenzionalmente non operativa per non simulare scrittura su API assenti."
    />
  );
}
